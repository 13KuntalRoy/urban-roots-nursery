# 🌿 UrbanRoots Nursery

A full-stack nursery web application — React frontend + Django REST API backend, served by Caddy, deployed on Render.

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/13KuntalRoy/urban-roots-nursery)

---

## ✨ Features

- 🌳 **Tree Catalog** — Browse premium trees with pricing, categories, and images
- ⚙️ **Site Config** — Manage site name, WhatsApp number, address, and hero text via admin
- 📋 **Order Steps** — Customisable "How to Order" guide
- ⭐ **Features Section** — Highlight your nursery's key selling points
- 🔐 **Django Admin** — Beautiful admin powered by [django-unfold](https://unfoldadmin.com/)
- 🚀 **Single Docker image** — Frontend + Backend + Caddy reverse proxy in one container

---

## 🚀 One-Click Deploy to Render

1. Click the **Deploy to Render** button above
2. Connect your GitHub account if prompted
3. Render will ask you to fill in two secret values:
   - `DJANGO_SUPERUSER_EMAIL` → your admin email (e.g. `you@example.com`)
   - `DJANGO_SUPERUSER_PASSWORD` → a strong password (min 8 chars, not all numeric)
4. Click **Apply** — Render will build and deploy automatically (~5 min)
5. Once live, visit:

| URL | Description |
|-----|-------------|
| `https://urbanroots-nursery.onrender.com/` | Frontend |
| `https://urbanroots-nursery.onrender.com/admin/` | Admin panel |
| `https://urbanroots-nursery.onrender.com/api/trees/` | Trees API |
| `https://urbanroots-nursery.onrender.com/api/config/` | Site config API |
| `https://urbanroots-nursery.onrender.com/api/features/` | Features API |
| `https://urbanroots-nursery.onrender.com/api/order-steps/` | Order steps API |

**Admin credentials:** username `admin` + the password you set above.

---

## 🛠 Local Development

### Prerequisites
- [Docker Desktop](https://www.docker.com/products/docker-desktop/)

### Quick start

```bash
# 1. Clone the repo
git clone https://github.com/13KuntalRoy/urban-roots-nursery.git
cd urban-roots-nursery

# 2. Create your local env file
cp .env.example .env
# Edit .env — set SECRET_KEY and superuser credentials

# 3. Start everything
docker compose up --build

# 4. Visit http://localhost
```

---

## 🏗 Architecture

```
Browser
  │
  ▼
Caddy (port 80)
  ├── /static/*  →  Django staticfiles
  ├── /media/*   →  Django media uploads
  ├── /api/*     →  Gunicorn (Django REST API, port 8000)
  ├── /admin/*   →  Gunicorn (Django admin, port 8000)
  └── /*         →  React SPA (built with Vite)
```

---

## 📁 Project Structure

```
urban-roots-nursery/
├── backend/               # Django project
│   ├── core/              # Models, views, serializers, admin
│   ├── root/              # Settings, URLs
│   └── requirements.txt
├── bloom-branch-boutique-main/  # React + Vite frontend
├── Dockerfile             # Multi-stage build (Node → Python+Caddy)
├── entrypoint.sh          # Boot script (migrate → superuser → serve)
├── Caddyfile              # Reverse proxy config
├── docker-compose.yml     # Local dev stack
├── render.yaml            # Render IaC (one-click deploy)
└── .env.example           # Environment variable template
```

---

## 🔑 Environment Variables

| Variable | Required | Description |
|----------|----------|-------------|
| `SECRET_KEY` | ✅ | Django secret key (auto-generated by Render) |
| `DEBUG` | ✅ | `True` for local, `False` in production |
| `ALLOWED_HOSTS` | ✅ | Comma-separated allowed host names |
| `DATABASE_URL` | ✅ | PostgreSQL connection string |
| `DJANGO_SUPERUSER_USERNAME` | ✅ | Admin username (default: `admin`) |
| `DJANGO_SUPERUSER_EMAIL` | ✅ | Admin email |
| `DJANGO_SUPERUSER_PASSWORD` | ✅ | Admin password |
